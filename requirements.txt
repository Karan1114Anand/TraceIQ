# ============================================================
# TraceIQ — Requirements
# Local-first: Ollama (text gen) + HuggingFace (embeddings)
# No cloud API keys required.
# ============================================================

# ── Web API (backend server) ──────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # required for FastAPI file uploads

# ── LLM / Orchestration ───────────────────────────────────
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0
ollama>=0.2.0

# ── Embeddings (local — no API key needed) ────────────────
sentence-transformers>=2.7.0
transformers>=4.40.0
torch>=2.0.0                   # CPU build sufficient; GPU optional

# ── Vector Store ──────────────────────────────────────────
chromadb>=0.5.0

# ── Keyword / Sparse Retrieval ────────────────────────────
rank-bm25>=0.2.2

# ── Document Parsers ──────────────────────────────────────
pdfplumber>=0.11.0
pymupdf>=1.24.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0

# ── Config / Env ──────────────────────────────────────────
pydantic>=2.0.0
python-dotenv>=1.0.0

# ── Utilities ─────────────────────────────────────────────
loguru>=0.7.0
rich>=13.0.0
requests>=2.31.0
typing_extensions>=4.10.0
certifi>=2024.0.0              # Windows SSL fix
